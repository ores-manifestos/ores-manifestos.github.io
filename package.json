{
  "name": "ores-manifestos-site",
  "description": "The ORES Manifestos — the engineering principles this workspace is actually built on. ores-manifestos.github.io",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.6.0"
  }
}
